# 01 raccourci clavier

`w` déplacer un objet

`e` Rotation

`r` Déformation échelle

`t` Boite

`y` transformation

`option` main provisoire (repare quand on relâche)

`q` main total

`cmd` + `d` dupliquer un composant